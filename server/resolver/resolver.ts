export default {
    Query: {
        // users: () => usersQuery(),
    },
    Mutation: {
        // signin: (parent: any, args: User) => signinMutation(args),
        // login: (parent: any, args: User) => loginMutation(args)
    },
};
